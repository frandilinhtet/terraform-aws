# 🚀 AWS VPC + RDS setup (Terraform)

This Terraform project creates a vpc and RDS instance with private subnet.

## Features
- VPC
- Public Subnets
- Private Subnets
- MySQL RDS 
- Fully parameterized (no hard-coded values)

---

## 🧰 Requirements

- Terraform ≥ 6.0
- AWS CLI configured (`aws configure`)
- IAM permissions for VPC and RDS resources

---

## 📦 Setup

Clone the repo:

```sh
git clone https://github.com/frandilinhtet/terraform-aws.git
cd terraform-rds
