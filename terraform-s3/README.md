# 🚀 AWS S3 Static Website Hosting (Terraform)

This Terraform project creates an S3 bucket configured for **static website hosting** with **public read access**.

## Features
- S3 bucket
- Static website hosting enabled
- Public website endpoint output
- Uploads `index.html`
- Fully parameterized (no hard-coded values)

---

## 🧰 Requirements

- Terraform ≥ 6.0
- AWS CLI configured (`aws configure`)
- IAM permissions for S3 resources

---

## 📦 Setup

Clone the repo:

git clone https://github.com/frandilinhtet/terraform-aws.git
cd terraform-s3
